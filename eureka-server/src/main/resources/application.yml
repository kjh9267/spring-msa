server:
  port: 8761

spring:
  application:
    name: eureka-server


eureka:
  instance:
    prefer-ip-address: true
  server:
    response-cache-update-interval-ms: 1000
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka